const initialState = {
  name: "",
  logo: "",
};

export default function configReducer(state = initialState, action: { type: ""; value: {} | null }) {
  switch (action.type) {
    default:
      return state;
  }
}
